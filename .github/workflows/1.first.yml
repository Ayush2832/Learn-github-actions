# This is first example of the github action file.
# Workflow dispatch is the manual way to run the job
name: HelloWorld
on:
  workflow_dispatch: 
jobs:
  say-hello-to-bash:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello-world"
